pipeline {

    agent any

    stages {
        stage('Run') {
            steps {
                echo ${WORKSPACE}
                sh '''
                    pip
                '''
            }
        }
    }
}
